import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class PerfilService {

  constructor() { }

  async cargarinfoUser(){
    // const user = await this.storage.obtenerUsuario();
    // const userEmail = await this.auth.currentUser;
    // const userFilter = user.filter(e => e.correo == userEmail?.email);
    // await 

    //console.log("property", this.storage.userCorreo);
    // this.usuario = (await this.storage.obtenerUsuario()).filter(e => e.correo == userEmail?.email);
    // console.log("USUARIO FILTRADO",this.usuario);
    
  }
}
